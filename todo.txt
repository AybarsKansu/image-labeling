train logic, train file structure
change train button logic, can be selected only if a model is selected


>> modellerin type'ı yerine detaylı kullanım rehberi gelebilir.
>> backende storelanırken yolo formatında storelanıyor. toon'a çevrilmesi gerekebilir.